(function() {	//enclosed anonymous function to prevent variable from being accessed in global scope
  'use strict';

  angular
    .module('app', [ //site dependencies 
      'ui.router',
      'mgcrea.ngStrap',
      'ngCookies'
      ])

})();